# Lambda + Cognito + API Gateway (OAuth Scopes)

This example demonstrates how to deploy a serverless API
that uses **Cognito access tokens** with **OAuth scopes**
for authorization.

## Architecture
